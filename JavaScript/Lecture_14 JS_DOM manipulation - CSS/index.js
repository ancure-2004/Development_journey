/******* Changing CSS using style method ********/
// let paraElement = document.getElementById("spara"); // --> fetching an element
// console.log(paraElement.style)   // --> getting using style
// paraElement.style.background = "blue" // --> setting using style
// paraElement.style.color = "White"

/******* Changing CSS using cssText ********/
// let secondDiv = document.getElementById("sdiv")
// secondDiv.style.cssText  // --> get using cssText
// secondDiv.style.cssText = "background-color: black; color: white; padding : 0.5rem" // --> setting using cssText

/******* Changing CSS using setAttribute ********/
// let firstElemet = document.querySelector("#fdiv");
// firstElemet.setAttribute("class", "secondDivClass");  // --> to change the attribut
// firstElemet.setAttribute("style", "red");

/******* Changing CSS using className ********/
// let fpara = $0
// fpara.className // --> fetching class
// fpara.className = "Gohan Bulbul" // --> setting the class or changing it

/******* Changing CSS using classList ********/
// let fpara = document.querySelector("#fpara")
// fpara.classList // --> fetching the details of class
// fpara.classList.add('thirdclass')  // --> add new class
// fpara.classList.remove('firstClass') // --> remove the particular class
// fpara.classList.toggle("secondClass") // --> toggle the class first time toggling will remove the class
// fpara.classList.toggle("secondClass") // --> second time toggling will add the class
// fpara.classList.contains('firstClass') // --> check if the class if there or not
